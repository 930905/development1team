<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.zerock.mapper.FreeBoardMapper">
	
	<!-- select 태그의 id 속성값은 메서드의 이름과 일치하게 작성합니다.
     resultType 속성의 값은 select 쿼리의 결과를 특정 클래스의 객체로
 	          만들기 위해서 설정합니다. -->	
  	<!-- select free_bno, free_title, free_writer, free_regdate, free_updatedate from free_board where free_bno > 0  -->

	<select id="getList" resultType="org.zerock.domain.FreeBoardVO">
	<!-- MyBatis xml 파일 작성에 사용한 CDATA 부분은 XML에서 부등호를
	          사용하기 위해서 사용합니다. -->
	<![CDATA[
		select bno, title, writer, regdate, updatedate from free_board where bno > 0
	]]>	
	</select>
	
</mapper>
