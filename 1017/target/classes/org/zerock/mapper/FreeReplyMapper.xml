<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="org.zerock.mapper.FreeReplyMapper">


	<select id="read" resultType="org.zerock.domain.FreeReplyVO">
		SELECT * from free_reply WHERE rno = #{rno}	
	</select>
	
	 

	<delete id="delete">
		DELETE from free_reply WHERE rno = #{rno}	
	</delete>
	

	<update id="update">
		UPDATE free_reply set reply = #{reply}, updatedate = sysdate WHERE rno = #{rno}
	</update>
	

	 
	 
	  <select id="getListWithPaging" resultType="org.zerock.domain.FreeReplyVO">
	  <![CDATA[
	  select rno, bno, reply, replyer, replydate, updatedate
	  from
		(
		select /*+ INDEX(free_reply idx_reply) */
		rownum rn, rno, bno, reply, replyer, replyDate, updatedate
		from free_reply
		where bno = #{bno}  
		and rno > 0
		and rownum <= #{cri.pageNum} * #{cri.amount}
		) where rn > (#{cri.pageNum} - 1) * #{cri.amount}
	 ]]>	 
	 </select>
	 

	 <select id="getCountByBno" resultType="int">
	 <![CDATA[
	 	SELECT COUNT(RNO) FROM free_reply WHERE BNO = #{bno}
	 ]]> 
	 </select>

</mapper>
